# LSDAI Simplified - Multi-WebUI Launcher Requirements

# Core dependencies
requests>=2.25.0
tqdm>=4.60.0

# Optional dependencies for enhanced functionality
ipywidgets>=7.0.0          # Interactive widgets (Linux/macOS)
gitpython>=3.0.0          # Git operations (optional)

# Development dependencies (optional)
pytest>=6.0.0             # Testing
black>=21.0.0             # Code formatting
flake8>=3.8.0             # Linting

# Note: The system works with just Python 3.8+ and requests
# All other dependencies are optional or have fallbacks